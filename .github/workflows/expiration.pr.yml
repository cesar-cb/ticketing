name: expiration-pull-request

on:
  pull_request:
    paths:
      - "expiration/**"

defaults:
  run:
    working-directory: expiration

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: docker-compose up -d
      - run: yarn && yarn lint && yarn test
      - run: docker-compose down
